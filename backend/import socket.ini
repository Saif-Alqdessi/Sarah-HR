import socket
try:
    # استبدل 'your-project-id' بالـ ID تبعك الموجود في رابط سوبابيس
    host = "your-project-id.supabase.co" 
    print(f"Checking connection to {host}...")
    socket.gethostbyname(host)
    print("✅ DNS is working!")
except Exception as e:
    print(f"❌ DNS Error: {e}")